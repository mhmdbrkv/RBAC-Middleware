<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Something Went Wrong</title>
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: linear-gradient(135deg, #ff6a6a, #ffa07a);
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }

      .container {
        background-color: #fff;
        max-width: 800px;
        width: 100%;
        margin: 20px;
        padding: 40px 30px;
        border-radius: 16px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        animation: slideUp 0.6s ease-out;
        text-align: center;
      }

      .error-icon {
        font-size: 4rem;
        color: #e74c3c;
        margin-bottom: 20px;
      }

      .error-title {
        font-size: 2.4rem;
        color: #333;
        margin-bottom: 10px;
      }

      .error-subtitle {
        font-size: 1.1rem;
        color: #666;
        margin-bottom: 20px;
      }

      .error-message,
      .error-code {
        background-color: #fdecea;
        color: #c0392b;
        padding: 12px 18px;
        border-radius: 8px;
        margin: 10px auto;
        max-width: 500px;
        font-weight: 500;
      }

      .error-description {
        color: #555;
        font-size: 1rem;
        line-height: 1.6;
        margin: 30px 0;
      }

      .action-buttons {
        display: flex;
        justify-content: center;
        gap: 20px;
        flex-wrap: wrap;
        margin-bottom: 30px;
      }

      .btn {
        padding: 12px 24px;
        border-radius: 8px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        min-width: 130px;
        text-align: center;
        transition: all 0.3s ease;
      }

      .btn-primary {
        background-color: #4caf50;
        color: white;
        border: none;
      }

      .btn-primary:hover {
        background-color: #388e3c;
      }

      .btn-secondary {
        background-color: #e74c3c;
        color: white;
        border: none;
      }

      .btn-secondary:hover {
        background-color: #c0392b;
      }

      .error-details {
        margin-top: 30px;
        text-align: left;
      }

      .error-details h3 {
        color: #333;
        margin-bottom: 10px;
        font-size: 1.3rem;
      }

      .error-details ul {
        padding-left: 20px;
        color: #555;
        line-height: 1.8;
        font-size: 0.95rem;
      }

      @keyframes slideUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @media (max-width: 600px) {
        .container {
          padding: 25px 20px;
        }

        .action-buttons {
          flex-direction: column;
        }

        .btn {
          width: 100%;
        }

        .error-details ul {
          padding-left: 15px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="error-icon">⚠️</div>

      <h1 class="error-title">Oops! Something Went Wrong</h1>
      <p class="error-subtitle">We encountered an unexpected error</p>

      <!-- EJS error message display -->
      <% if (typeof message !== 'undefined' && message) { %>
      <div class="error-message"><%= message %></div>
      <% } %> <% if (typeof error !== 'undefined' && error) { %>
      <div class="error-code">Error Code: <%= error.status || 500 %></div>
      <% } %>

      <p class="error-description">
        We're sorry for the inconvenience. Our team has been notified and is
        working to resolve this issue. Please try again in a few moments or
        contact support if the problem persists.
      </p>

      <div class="action-buttons">
        <a href="/" class="btn btn-primary">Go Home</a>
        <button onclick="window.location.reload()" class="btn btn-secondary">
          Try Again
        </button>
      </div>

      <div class="error-details">
        <h3>What happened?</h3>
        <ul>
          <li>The server encountered an internal error</li>
          <li>This could be a temporary issue with our services</li>
          <li>Your data and account remain safe</li>
          <li>Our technical team has been automatically notified</li>
        </ul>
      </div>
    </div>
  </body>
</html>
